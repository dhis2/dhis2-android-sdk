up:
- ALTER TABLE DataSet ADD COLUMN workflow TEXT;
- CREATE TABLE DataApproval (_id INTEGER PRIMARY KEY AUTOINCREMENT, attributeOptionCombo TEXT NOT NULL, period TEXT NOT NULL, organisationUnit TEXT NOT NULL, state TEXT NOT NULL, workflow TEXT NOT NULL, FOREIGN KEY (attributeOptionCombo) REFERENCES CategoryOptionCombo (uid) ON DELETE CASCADE DEFERRABLE INITIALLY DEFERRED, FOREIGN KEY (period) REFERENCES Period (periodId), FOREIGN KEY (organisationUnit) REFERENCES OrganisationUnit (uid) ON DELETE CASCADE DEFERRABLE INITIALLY DEFERRED, UNIQUE (attributeOptionCombo, period, organisationUnit, workflow));